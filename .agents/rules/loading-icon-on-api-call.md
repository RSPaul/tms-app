---
trigger: always_on
---

When there is data post or form is submit, and data is sent to backend API or fetched from API then you should show and loading transition inside the submit button until the API response back or loading icon on the page based on the data is being sent to API.