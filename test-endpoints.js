async function test() { const login = await fetch('http://localhost:3000/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'john@test.com', password: 'password123' }) }); const cookie = login.headers.get('set-cookie'); console.log('Login:', login.status, cookie); const dash = await fetch('http://localhost:3000/api/consultant/dashboard', { headers: { 'Cookie': cookie } }); console.log('Dash:', dash.status, await dash.text()); } test();
